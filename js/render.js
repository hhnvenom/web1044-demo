// ============================================================
//  render.js — ĐỔ DỮ LIỆU RA HTML      (buổi 6 · LAB 6.3, 6.4)
// ============================================================
//  Quy tắc vàng: hàm render chỉ ĐỌC dữ liệu rồi vẽ ra màn hình.
//  Không sửa dữ liệu trong này.
// ============================================================

// TODO: renderSanPham(danhSach) — vẽ danh sách sản phẩm vào #product-list
// Gợi ý: dùng map() để tạo chuỗi HTML rồi gán một lần vào innerHTML,
//        nhanh hơn nhiều so với appendChild từng cái.

// TODO: renderDanhMuc() — vẽ các nút lọc vào #filters từ mảng CATEGORIES

// TODO: renderGioHang(cart) — vẽ giỏ hàng vào #cart-items và cập nhật #cart-total
// Nhớ: giỏ trống phải hiện thông báo, KHÔNG để vùng trắng.

// TODO: capNhatSoLuongTrenHeader(cart) — hiện số món trong giỏ ở header
