# l4/end — Bài 4 hoàn chỉnh

**Slide:** Bài 4 · "Tóm tắt bài 4" (demo `l4-end`).

Toàn bộ demo bài 4 trong `index.js`:

1. Mảng cơ bản — push/pop/unshift/shift (`l4/d1-mang-co-ban`)
2. Object — truy cập, sửa, thêm thuộc tính (`l4/d2-object-co-ban`)
3. `map`, `filter`, `reduce` (`l4/d3-map-filter-reduce`)
4. `sort` và bẫy so sánh chuỗi (`l4/d4-sort`)
5. Nối chuỗi phương thức (`l4/d5-chain`)
6. `splice` / `slice`, `for...of` / `forEach`, `find` / `findIndex`, mảng đối tượng, JSON

```bash
node index.js
```

Đây là điểm khởi đầu của bài 5. Lab 04: xem branch `l4/lab-start`.
