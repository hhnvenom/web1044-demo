# l2/end — Bài 2 hoàn chỉnh

**Slide:** Bài 2 · "Tóm tắt bài 2" (demo `l2-end`), và cũng là slide mở đầu bài 3.

Toàn bộ code demo của bài 2 trong `index.js`, theo đúng thứ tự trên slide:

1. `if...else` chẵn / lẻ (`l2/d1-if-else`)
2. `if` lồng nhau so với `else if` (`l2/d2-if-else-nested`)
3. `switch...case` và bẫy quên `break` (`l2/d3-switch-case`)
4. Vòng lặp lồng nhau — bảng cửu chương (`l2/d4-vong-lap-long`)
5. Toán tử ba ngôi, `&&` `||`, `while`, `do...while`, `break`, `continue`
6. Tính tiền điện bậc thang — đoạn code mở đầu bài 3

```bash
node index.js
```

Đây là điểm khởi đầu của bài 3. Lab 02: xem branch `l2/lab-start`.
