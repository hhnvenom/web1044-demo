# l5/d3-class-constructor — Từ object literal đến class

**Slide:** Bài 5 · demo `l5-d3`.

Bốn bước của animation trên slide: object literal → bọc thành `class` → tạo instance bằng `new` → một class đúc ra bao nhiêu object cũng được.
Phần 3b cho thấy `constructor` chạy đúng một lần cho mỗi `new`.

```bash
node index.js
git diff l5/d2-function-constructor l5/d3-class-constructor
```

Branch tiếp theo: `l5/d4-class-student`.
