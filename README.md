# l8/d3-dark-mode — Dark / Light mode nhớ lựa chọn

**Slide:** Bài 8 · "Dark mode nhớ lựa chọn" (demo `l8-d3`).

Ba dòng đầu của `js/app.js` là quan trọng nhất: **đọc lại lựa chọn cũ ngay khi tải trang**.
Chỉ viết phần lưu mà quên phần đọc lại là lỗi phổ biến nhất của LAB 8.1.

```bash
npx serve .
# rồi mở http://localhost:3000
git diff l8/d1-local-storage l8/d3-dark-mode
```

Branch tiếp theo: `l8/d4-cart-persist`.
