# l4/d1-mang-co-ban — Thêm và xoá phần tử

**Slide:** Bài 4 · "Thêm và xoá phần tử" (demo `l4-d1`).

`push` và `pop` làm ở **cuối**, `unshift` và `shift` làm ở **đầu**.
`pop` và `shift` **trả về** phần tử vừa xoá — đúng yêu cầu d của LAB 4.1.

```bash
node index.js
git diff l3/end l4/d1-mang-co-ban
```

Branch tiếp theo: `l4/d2-object-co-ban`.
