# l4/d5-chain — Nối chuỗi phương thức

**Slide:** Bài 4 · "Nối chuỗi phương thức" (demo `l4-d5`).

`filter` → `sort` → `map`. Mỗi bước trả về mảng mới nên nối tiếp được.
Lọc trước thì hai bước sau chạy trên ít phần tử hơn.

```bash
node index.js
git diff l4/d4-sort l4/d5-chain
```

Branch tiếp theo: `l4/end`.
