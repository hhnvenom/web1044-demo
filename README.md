# l1/end — Bài 1 hoàn chỉnh

**Slide:** Bài 1 · Biến, kiểu dữ liệu và ép kiểu (demo `l1-end`).

Toàn bộ code demo của bài 1 gộp trong `index.js`, theo đúng thứ tự trên slide:

1. File JS đầu tiên (`l1/d1-hello-console`)
2. Ép kiểu ngầm định (`l1/d2-ep-kieu`)
3. Ép kiểu tường minh (`l1/d3-ep-kieu-tuong-minh`)
4. `let` / `const` / `var`, `typeof`, giá trị falsy, `==` vs `===`

```bash
node index.js
```

Đây là điểm khởi đầu của bài 2. Lab 01: xem branch `l1/lab-start`.
