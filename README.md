# l3/end — Bài 3 hoàn chỉnh

**Slide:** Bài 3 · "Tóm tắt bài 3" (demo `l3-end`).

Toàn bộ code demo của bài 3 trong `index.js`, theo đúng thứ tự trên slide:

1. Tách code lặp thành hàm (`l3/d2-tach-ham`)
2. Ba cách khai báo hàm (`l3/d3-cac-cach-khai-bao`)
3. Hàm gọi hàm — `layHeSo` và `tinhLuong` (`l3/d8-ham-goi-ham`)
4. Tham số, đối số, giá trị mặc định
5. `return` so với `console.log`
6. Phạm vi biến trong và ngoài hàm
7. Call stack — A gọi B gọi C

```bash
node index.js
```

Đây là điểm khởi đầu của bài 4. Lab 03: xem branch `l3/lab-start`.
