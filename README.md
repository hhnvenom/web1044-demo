# l2/d6-toan-tu-logic — Toán tử logic và toán tử ba ngôi

**Slide:** Bài 2 · "Toán tử logic" (demo `l2-d6`).

`&&` cả hai phải đúng · `||` chỉ cần một cái đúng · `!` đảo ngược.
Toán tử ba ngôi viết `if...else` gọn trên một dòng.

```bash
node index.js
git diff l2/d5-vong-lap-tung-buoc l2/d6-toan-tu-logic
```

Branch tiếp theo: `l2/d7-while-do-while`.
